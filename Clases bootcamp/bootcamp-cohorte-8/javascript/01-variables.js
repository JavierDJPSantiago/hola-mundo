/*
variables: nos sirven para almecenar informacion.
var y let pueden ser iguales pero difieren en casos, hoy se pide let
*/

var nombre = 'Carlos Eduardo';
let edad = 31;
var mascotas = false;

//podemos usar comas para concatenar
console.log(nombre, edad, mascotas);
//o usar +, pero no nos dara espacios y se amontona
console.log(nombre + edad + mascotas);
